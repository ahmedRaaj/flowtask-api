# Copy this file to .env and fill in your own local values.
# .env is git-ignored and must never contain real production credentials.

POSTGRES_DB=flowtask
POSTGRES_USER=ahmedraaj
POSTGRES_PASSWORD=ahmedraaj
POSTGRES_PORT=5432
